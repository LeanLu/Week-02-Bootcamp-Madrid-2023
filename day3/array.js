const aData = [1, 2, 3, 4, 5, 'Pepe'];

console.log(aData);
console.log(aData.length);

// Métodos del Array:
// aData.indexOf(4);

/* Le puedo agregar una propiedad en cualquier posición y quedan las intermedias vacías.
aData[35] = 12;
console.log(aData);
console.log(aData.length);
*/

// Declaración de función:
// function foo1() {}

// Asignación

// De función anónima:
// const foo2 = function () {};
// Si le quiero poner nombre:
// const foo3 = function nameFunction() {};

// Arrow function:
// const foo4 = () => {};

// Ejemplo de función para Array con un parámetro
// const aLength = (aData) => {};

// Ejemplo de función para Array con dos parámetros
// const aPush = (aData, algo) => {};
